# 📈 ÉVOLUTION KALAMUNDI
### Journal daté de ce qui est construit et décidé

*Créé le 15 juillet 2026.*
*Règle : on ajoute une entrée à chaque évolution significative (feature, décision, correctif majeur) — jamais en lot rétroactif. Entrée la plus récente en haut.*

Format : `AAAA-MM-JJ — [type] description`
Types : `feat` (fonctionnalité) · `decision` (choix produit) · `fix` (correctif) · `perf` · `docs` · `infra`

---

## 2026-07

- **2026-07-21 — feat** : **attribution des ventes aux campagnes**. Migration `V018__campagnes_conversions.sql` : `paiements.campagne_id` relie une vente Fapshi à sa campagne. `/api/fapshi-pay` relit déjà le prix campagne côté serveur et inscrit maintenant la campagne sur le paiement ; `/api/fapshi-webhook` incrémente les conversions et `revenu_xaf` de la campagne quand le paiement est confirmé, de façon idempotente. Le dashboard admin Campagnes affiche donc vues, clics, intentions d'achat, conversions et revenu.
- **2026-07-21 — fix** : **wrapper Android réaligné pour publication publique Kalamundi**. L'ancien projet `android-owner` ouvrait `kalamundi.pages.dev/pages/owner.html`, s'appelait « Kalamundi Owner » et gardait un package `com.kalamundi.owner` — inutilisable comme app grand public Play Store. Il pointe désormais vers `https://kalamundi.afrisaas.com/`, devient `com.kalamundi.app`, affiche « Kalamundi / La Plume du Monde », autorise les domaines Kalamundi/Supabase/Fapshi dans la WebView, et `gradlew.bat` n'utilise plus un chemin Gradle cassé dans le profil utilisateur mais le zip Gradle local. Contrôle `check-android-wrapper`. **Limite** : la génération AAB/APK signée Play Store reste à faire dans Android Studio avec un keystore de release (secret hors dépôt) ; le build CLI local dépasse le délai dans l'environnement Codex.
- **2026-07-20 — feat** : **campagnes de vente livre livrées (D17)**. Nouvelle migration `V017__campagnes_vente.sql` : table `campagnes_vente` avec œuvre/offre/auteur, dates, statut, slug public, prix campagne, prix barré, budget pub, canaux sociaux, conditions admin/auteur et compteurs vues/clics/intention d'achat. Nouvelle page publique `/pages/campaign.html?c=slug` pensée pour les pubs Facebook/WhatsApp/TikTok : couverture, argumentaire, prix, partage et bouton d'achat Fapshi. Accueil enrichi d'un rail **« En campagne cette semaine »** affiché seulement si des campagnes actives existent. Admin enrichi d'une section **Campagnes livre** pour créer/activer/suspendre et copier le lien public. `payment.js` respecte le prix spécial d'une campagne, et `/api/fapshi-pay` le relit côté serveur depuis Supabase avant d'initier Fapshi (l'URL ne fait jamais foi pour le montant). SW `kala-v34`.
- **2026-07-20 — fix** : **accueil plus compact et reprise de lecture dédupliquée**. Le rail « Reprendre la lecture » n'affiche plus plusieurs fois la même œuvre quand l'historique contient plusieurs lignes de lecture ; les cartes sans couverture uploadée utilisent désormais la couverture générée Kalamundi plutôt qu'un simple bloc initiale. Le carrousel auteurs ne défile plus artificiellement quand il contient trop peu de livres, ce qui évite l'impression de section vide/étirée vue en capture.
- **2026-07-20 — feat** : **accueil réorganisé selon la maquette validée — l'auteur au centre**. `index.html`/`app.js` réécrits : un seul rail **« À découvrir — nos auteurs »** (ne montre QUE les créations Kalamundi, jamais le patrimoine, avec une tuile « Ta place est ici » en CTA permanent) remplace 4 sections qui mélangeaient tout (Pourquoi Kalamundi, Œuvres du moment, Rayons de lecture 4-rails, Explorer par genre, Nouvelles publications, Nouveaux talents/spotlight) — coupées ou fusionnées. Nouvelle section **Occasion** (vrai état vide si aucune annonce, jamais inventé) et **Bibliothèque patrimoine** (séparée, discrète, légende dynamique). **Bug corrigé au passage** : `exclureSysteme` ne filtrait rien (0/288 exclues, comparait à un seul UUID alors que chaque lot d'import en a un différent) — affectait aussi les « Rayons de lecture » et le filtre catalogue `collection=originaux`, pas seulement l'accueil ; corrigé une fois dans `getOeuvres()`, tous les appelants en profitent. Code mort supprimé (`chargerVedettes/Nouveautés/NouveauxTalents/RailsCommerce`, `getRailsMarchands/getNouveauxAuteurs/getOeuvresPremiersPas`). SW `kala-v33`. **Diagnostic en cours de route** : le Service Worker servait un `app.js` en cache pendant les tests, masquant les changements malgré un serveur à jour (résolu en désinscrivant le SW pour tester, et en bumpant `VERSION` pour la prod). `check-gift-flow.mjs` réparé (mock manquant pour `getCommandesOccasion`, ajouté plus tôt dans la session). `check-commerce-rails.mjs` réécrit pour vérifier la nouvelle structure + interdire le retour du code supprimé. Tout vérifié en direct dans le navigateur contre la vraie base Supabase (3 auteurs, 285 patrimoine, 0 occasion active, 288 total) et console sans erreur. `npm run check` : 24 contrôles verts. Détails `ERROR_LOG.md`.

- **2026-07-20 — fix ⚠️** : **protège la rémunération des auteurs face à l'abonnement**. Franklin a signalé qu'un abonné Reader+ pouvait lire n'importe quelle œuvre premium gratuitement sans que l'auteur soit payé — conséquence non voulue du correctif Reader+ du même jour. `verifierAccesPremium()` exige désormais l'abonnement actif **ET** un opt-in explicite « Kalamundi Select » de l'œuvre (nouveau `oeuvreEstEnAbonnement()`, table `livre_offres.type='lecture_abonnement'`, prévue dès `V007` mais jamais utilisée). Tant qu'aucun auteur n'a opté et que le fonds mensuel (§5.2 `ADAPTATION_STANDARDS_KDP.md`) n'existe pas, l'accès abonnement reste fermé partout — comportement vérifié en direct contre la base réelle. Contrôle `check-abonnement-perks` réécrit pour interdire toute régression vers l'accès abonnement sans Select. Détails `ERROR_LOG.md`. **Reste à construire** : l'opt-in Select côté auteur (UI) et le fonds mensuel de redistribution à la page lue.

- **2026-07-20 — decision ✅** : **vente du plan Institution suspendue** (Franklin choisit l'option 3). Bloqué sur 3 fronts : page (carte « Bientôt disponible » + contact institutions@kalamundi.com), client (`payment.js`, plan retiré + message clair), et surtout **serveur** (`fapshi-pay.js` rejette 403 `abonnement_institution` — le vrai verrou contre un appel API direct). 0 abonné payant existant, aucun impact. Contrôle `check-institution-suspendu`.
- **2026-07-20 — audit** : plan **Institution (10 000 FCFA/mois) audité — aucune promesse livrée**. Contrairement à Reader+/Auteur Pro (bugs de câblage, corrigés), `institution.js` (inscription libre d'établissement) et le paiement `profiles.abonnement='institution'` sont deux systèmes totalement déconnectés ; la table `institutions` n'a même pas de modèle de données pour plusieurs membres. **Décision requise de Franklin** : construire la vraie fonctionnalité équipe, réduire les promesses affichées, ou suspendre la vente du plan. Aucun code changé — un correctif superficiel serait trompeur. Détails `ERROR_LOG.md`.

- **2026-07-20 — fix** : **les avantages Reader+/Auteur Pro sont désormais réellement honorés**, pas juste vendus. Audit du code (pas de la page marketing) déclenché par une question de Franklin sur la valeur d'un abonnement — a révélé que « accès illimité aux œuvres premium » et « aucune publicité » étaient tous deux non fonctionnels (le premier jamais implémenté, le second cassé par une divergence de champ entre le paiement et le gating). Nouveau `api.aAbonnementActif(userId, plans)` en source de vérité unique, consommé par `verifierAccesPremium()` (accès premium) et `app.js` (gating pub). Contrôle `check-abonnement-perks`. Détails dans `ERROR_LOG.md`. **Auteur Pro et Institution restent à auditer.**
- **2026-07-20 — docs** : maquette accueil corrigée avec des chiffres vérifiés en base (288 œuvres, 3 originales, 2 offres de prêt test, 0 annonce d'occasion active) au lieu de valeurs inventées — le rail occasion affiche un vrai état vide plutôt que des annonces fictives.

- **2026-07-19 — feat** : **lecteur façon Thorium — round 3 (mode défilement + TTS multilingue robuste)**. (1) **Mode défilement fonctionnel** (`.layout-mode-btn`, `etat.modeAffichage`) : le chapitre entier est rendu en un seul bloc scrollable au lieu d'être découpé en pages hauteur-écran (`_paginerContenu` court-circuité en mode `scroll`, 4e de couverture fusionnée à la suite du texte plutôt que sur une page séparée). **Trois bugs CSS en cascade** ont dû être corrigés pour que ça marche réellement (piège flexbox classique, documenté en commentaire dans `reader.css` pour ne pas le retrouver) : `min-height:auto` par défaut sur les flex-items fait grandir tout le conteneur au lieu de scroller ; `.reader-book-page` avait `max-height:100%/overflow:hidden` (pensé pour la pagination) qui tronquait le texte invisiblement ; et surtout `.reader-page` n'avait que `min-height:100vh` (un plancher, pas un plafond) — sans hauteur RÉELLEMENT bornée quelque part dans la chaîne flex, `overflow:auto` ne déclenche jamais de scroll interne, tout grandit à l'infini. Correctif final : `.reader-page.is-scroll-mode { height:100dvh }` couplé à `min-height:0` à chaque niveau. **Vérifié en direct** : `scrollHeight` (288259px) ≠ `clientHeight` (426px) confirmant un vrai scroll interne, barre de progression asservie au scroll (`scrollTop`/`scrollHeight`, testée à 17%), retour en pagination sans résidu de classe ni régression. (2) **TTS multilingue corrigé et durci** : `LANGUES_TTS` étendue aux 13 langues (les 11 de `LANGUES_LECTURE` + langues d'origine additionnelles) — avant, seules 9 étaient couvertes, japonais/coréen/hindi/chinois manquaient. **Bug corrigé** : la voix suivait `oeuvre.langue_originale` en dur, donc un livre traduit à l'affichage était lu avec la mauvaise voix — elle suit désormais `etat.langueAffichee` en priorité. Attente asynchrone des voix (`voiceschanged`, filet de sécurité 500ms) avant de conclure qu'aucune voix n'est installée pour une langue — évite un faux négatif au tout premier appel. Verrou anti-course (`_ttsGeneration`) si l'utilisateur arrête pendant l'attente des voix. Message explicite si aucune voix n'est trouvée pour la langue (lecture quand même avec la voix par défaut, plutôt qu'un échec silencieux qui ferait croire que « ça marche »). **Vérifié** : démarrage confirmé (`speechSynthesis.speaking === true`) même dans un navigateur sans aucune voix système installée (0 voix dans l'environnement de test) — pas de blocage. Cache PWA bumpé en `kala-v32`. **Session interrompue par un disque système à 0 octet libre** (erreur npm `ENOSPC` en tentant de relancer le serveur de preview local) — travail commité et poussé avant l'interruption ; reste à vérifier visuellement le mode défilement sur un vrai appareil/navigateur avec voix installées une fois l'espace disque libéré.
- **2026-07-19 — feat** : **lecteur façon Thorium — round 2 (police accessible, espacements, lecture à voix haute)**. Complète les manques identifiés après le round 1 (couverture/4e de couverture intégrées) : (1) **Police « Lisible »** (`pages/reader.html`, `assets/css/reader.css`) : 4e option dans le sélecteur de police, pile Verdana/Tahoma/Segoe UI (formes de lettres distinctes, largement disponibles) + espacement lettres/mots élargi par défaut — choix assumé de ne **pas** auto-héberger une police dédiée dyslexie (OpenDyslexic etc.) pour rester dans les conventions du dépôt (webfonts déjà limitées à Fraunces/Inter). (2) **Espacement des lettres et des mots** réglable (3 paliers chacun, même pattern que l'interligne existant), persisté en localStorage, appliqué en mode chapitres natif (pas en EPUB — epub.js ne gère pas ces styles par thème). (3) **Colonnes 1/2** : déjà couvert par le mode double-page existant (`estModeDoublePage()`, automatique ≥980px) — pas de nouveau réglage ajouté, l'équivalent Thorium existait déjà sans être documenté comme tel. (4) **Lecture à voix haute** (`#btn-tts`, Web Speech API native, aucune dépendance) : lit le texte de la page affichée, langue de la voix choisie depuis `oeuvre.langue_originale`, enchaîne automatiquement sur la page suivante à la fin de chaque page tant que la lecture est active, s'arrête proprement sur toute navigation manuelle (clavier/tactile/boutons — nouvelle fonction `_arreterTTSSiActif()` appelée à chaque point d'entrée manuel pour ne pas laisser la voix décrochée du texte affiché). Portée limitée au mode chapitres natif (le mode EPUB reste hors périmètre, comme le round 1). Cache PWA bumpé en `kala-v31`. **Vérifié en direct** : police et espacement confirmés par `getComputedStyle` sur un `&lt;p&gt;` réel de page (pas le conteneur, qui garde sa police par défaut — piège d'abord mal testé), TTS démarre/s'arrête proprement (`speechSynthesis.speaking` true→false), `npm run check` et `node --check` propres.
- **2026-07-19 — feat** : **lecteur façon Thorium — couverture et 4e de couverture intégrées au feuilletage**. La couverture (`#reader-cover`) n'est plus un simple splash qu'on quitte en cliquant « Commencer la lecture » sans retour possible : c'est désormais la **page 0 du livre**, atteignable en tournant la page comme n'importe quelle autre (flèche gauche/droite, geste de balayage tactile ajouté — `touchstart`/`touchend` sur tout le lecteur, absent jusqu'ici). Depuis la page 1 du chapitre 1, reculer ramène à la couverture au lieu de ne rien faire (`_pagePrev()` + bouton « précédent » qui n'est plus désactivé en tout début de livre). La 4e de couverture (déjà générée par `_creerQuatriemeCouverturePage`, existante mais découverte tardivement lors de l'exploration — elle n'apparaît qu'en pagination normale) reste la dernière page du parcours : le livre se lit désormais **d'un bout à l'autre en un seul geste continu, couverture à couverture**. Barre de progression recalculée pour refléter l'avancement dans le **livre entier** (pas seulement le chapitre courant comme avant) via une approximation par chapitre (pas de pré-pagination de tous les chapitres, coût réseau) ; cliquer dessus ouvre la table des matières (pas de saut direct à une page arbitraire — limite technique assumée, documentée en commentaire). Portée volontairement limitée au **mode chapitres natif** : le mode EPUB (epub.js) gère sa propre pagination/couverture en interne et n'a pas été touché. Cache PWA bumpé en `kala-v30` (règle CLAUDE.md). **Vérifié en direct dans le navigateur** (`.claude/launch.json` config `kalamundi`) : couverture → page 1 → retour couverture → re-entrée dans le livre, bouton « précédent » plus désactivé en page 1, barre de progression book-wide (11 % à la page 11/104), clic sur la barre ouvre la TOC. **Piège rencontré et à retenir** : le Service Worker (`kala-v29` à l'époque) servait une version en cache de `reader.js` pendant les premiers tests, faisant croire à un bug pendant un moment — toujours vider `caches`/désinscrire le SW avant de tester un changement du lecteur en local, en plus de bumper la version pour la prod.
- **2026-07-19 — feat** : **refonte accueil/nav — round 1 (arbitrages tranchés après benchmark Libby/Kindle/Kobo/NYPL/Spotify)**. (1) **Onglet Apprendre distingué visuellement** (`assets/css/layout.css`) : icône teintée `--color-accent-dark` en permanence, pas seulement à l'état actif — Apprendre est notre douve (annales/examens/répétiteur), il ne doit pas se perdre parmi 5 icônes neutres. (2) **Recherche toujours visible sur l'accueil** (`index.html`, `assets/css/home.css`) : barre de recherche dans le hero, juste sous le sous-titre, soumet vers `/pages/library.html?recherche=...` (corrige le défaut #1 pointé sur Libby : icônes de recherche mal comprises). (3) **Rail "Reprendre la lecture"** (`assets/js/app.js`, fonction `chargerReprendre`) : réutilise `api.getBibliotheque` (déjà triée par `updated_at` desc sur `lectures`, aucune nouvelle requête) pour afficher les 4 derniers livres en cours dès la connexion, tout en haut de l'accueil — corrige le défaut #1 cité contre l'accueil Kindle (page dédiée à la vente plutôt qu'à la reprise de lecture). Lien direct vers `reader.html?ch=<chapitre_courant>`, pas juste la fiche. Section masquée si non connecté ou aucun historique. **Arbitrages tranchés sans fusionner Explorer/Accueil** : Explorer pointait déjà vers `library.html` (le catalogue), donc la distinction "catalogue" vs "ma bibliothèque" (`offline.html`) déjà validée restait cohérente — aucun changement structurel de nav nécessaire au-delà de la teinte Apprendre. **Vérifié** : `node scripts/check-js.mjs` (103 fichiers), aperçu navigateur local (`.claude/launch.json` — config `kalamundi` ajoutée pointant vers `http-server C:/kalamundi -p 5501`) : recherche hero soumet bien vers le catalogue avec le paramètre, teinte accent confirmée par `getComputedStyle` (`rgb(169,124,14)` vs vert par défaut), section Reprendre correctement masquée hors connexion. **Reste** : le lecteur façon Thorium (couverture/4e de couverture comme vraies pages tournables) est volontairement reporté à une prochaine session — chantier plus lourd, mis de côté à la demande de Franklin. **Audit dark mode (arbitrage #4)** — audité par calcul de contraste WCAG en direct (`getComputedStyle` + luminance relative), pas par capture d'écran (l'outil de screenshot du Browser pane a échoué systématiquement dans cette session, cause non identifiée — page saine par ailleurs, 0 erreur console, requêtes réseau propres). **Deux vrais bugs trouvés et corrigés** (`assets/css/base.css`) : `--text-light` (texte tertiaire : dates, métadonnées) n'était **jamais redéfini** ni en `body.theme-dark` ni en `body.theme-sepia` — il gardait sa valeur pensée pour fond crème (`#8A8272`), exactement le piège "dark mode = simple inversion" identifié dans la recherche 2026. Résultat mesuré : contraste 4,66:1 en dark (limite AA à peine tenue) et **3,23:1 en sépia (sous le minimum AA 4.5:1)**. Correctif : nouvelles variables `--dark-text-tertiary: #7FA080` et `--sepia-text-tertiary: #6B5638`, assignées à `--text-light` dans chaque bloc de thème. Résultat après correctif : dark 6,12:1, sépia 5,92:1 — hiérarchie de texte (primaire/secondaire/tertiaire) maintenant cohérente sur les 3 thèmes (dark : 15,79 / 9,83 / 6,12 ; sépia : 11,64 / 6,09 / 5,92). Le reste du dark mode (fonds, bordures, cartes) était déjà correctement redéfini et mesuré excellent (contraste corps de texte 17,76:1).

- **2026-07-19 — feat** : **reste de P4 #14 (occasion) et P3 #12 (store) livrés**. (1) **Arbitrage litige admin** : migration `V015__arbitrage_litige.sql` — policy RLS `cmd_occasion_lecture_admin` (l'admin ne pouvait pas voir les commandes en litige jusqu'ici, seuls acheteur/vendeur le pouvaient) + RPC `resoudre_litige(commande, decision)` SECURITY DEFINER vérifiant `profiles.role='admin'` en interne (`clos` = fonds versés au vendeur, `rembourse` = versement gelé côté vendeur — le remboursement effectif à l'acheteur reste manuel via Fapshi, aucun compte de remboursement n'étant capturé à la commande). Nouvelle section admin **Litiges occasion** (`pages/admin.html`, `assets/js/admin.js` : `chargerLitiges`/`resoudreLitige`), méthodes `api.adminGetLitiges`/`adminResoudreLitige`. (2) **Orchestration payout serveur** : `functions/api/payout-occasion.js` — Pages Function protégée par secret partagé (`PAYOUT_TASK_SECRET`), lit les commandes `statut='clos' AND payout_statut='a_verser'`, verse chaque vendeur via `envoyerPayout` (`scripts/lib/fapshi-payout.js`, déjà codé mais jusqu'ici jamais appelé par rien), marque `payout_statut='verse'` de façon idempotente (condition `payout_statut=eq.a_verser` dans le PATCH). Branchée sur le cron horaire `kalamundi-cron` (nouvel appel après `expirer_emprunts`) — reste inerte tant que Fapshi n'a pas activé le payout live et que `PAYOUT_TASK_SECRET` n'est pas configuré (worker + Pages), échoue proprement sans rien casser en attendant. (3) **Page listant toutes les annonces d'occasion** : `pages/occasion.html` + `assets/js/occasion-listing.js` (`api.getToutesAnnoncesOccasion`), lien ajouté au footer. (4) **Prix barré / promo (D17)** : migration `V016__promo_prix_barre.sql` (`livre_offres.prix_barre`, nullable, pas de nouvelle colonne de royalties — une promo peut toujours ajuster `royalties_plateforme_pct` sur l'offre si besoin), badge « -X% » affiché sur la fiche œuvre (`work.js`) quand `prix_barre > prix`, nouvelle section admin **Promotions** pour le définir par offre. Portée volontairement limitée : le prix barré ne s'affiche pour l'instant que sur la fiche œuvre, pas encore sur les rails d'accueil ni le catalogue (`library.js`) — à étendre si le besoin se confirme. Contrôle `check-litige-promo-payout`. Non exercé en vrai : migrations V015/V016 pas encore appliquées sur Supabase à la fin de cette session (à faire comme pour V007-V014).
- **2026-07-19 — feat** : **P4 #15 — Emprunter / prêt numérique livré (V1 accès temporel, pas de DRM LCP)**. Migration `V014__pret_numerique.sql` : `acces_premium` gagne `expire_le`/`emprunt_id` (un prêt = une ligne temporaire dans la même table que l'achat, réutilise la porte d'accès existante `api.verifierAccesPremium` sans toucher au lecteur) ; tables `emprunts` (un prêt actif max par offre/utilisateur) et `emprunts_file_attente` ; RPC SECURITY DEFINER `emprunter_livre` (refuse si accès déjà possédé, signale `file_attente` si le fonds maison — `livre_offres.stock` — est complet), `rendre_livre` (retour anticipé, révoque l'accès, sert le suivant de la file), `rejoindre_file_attente`/`quitter_file_attente`, `expirer_emprunts` (cron horaire : expire les prêts échus, révoque l'accès, sert la file). Le « fonds maison » (D6) = les offres `livre_offres.type='pret_numerique'` créées côté admin (`stock` = exemplaires en parallèle, `duree_acces_jours` = durée, 14 j par défaut) — pas d'UI de création dédiée pour l'instant, seedé en base. Fiche œuvre (`work.js`) : la carte « Emprunt » n'est plus « Bientôt » — bouton Emprunter / statut de prêt en cours avec échéance et retour anticipé / position en file d'attente, sans exposer le nombre d'exemplaires restants (RLS masque les prêts des autres, cohérent avec la vraie incertitude d'une bibliothèque). Page bibliothèque (`offline.html`/`offline-page.js`) : nouvelle section **Mes emprunts** (échéance, retour en un clic), même schéma que « Mes achats ». Cron `kalamundi-cron` étendu pour appeler `expirer_emprunts` à chaque exécution horaire. Contrôle `check-emprunt`. **Décision volontaire** : la Bible/Planification visaient Readium **LCP** (vraie DRM bibliothèque) — non retenu pour ce V1 car ça exige un serveur de licences externe (EDRLab/Readium LCP Server) et des certificats hors de portée de ce dépôt ; le contrôle d'accès temporel applicatif (même mécanisme que l'achat, avec expiration) offre l'expérience utilisateur du prêt sans cette dépendance. LCP resterait pertinent si Kalamundi doit un jour prêter des œuvres **sous droits externes** (maisons d'édition, D13) où une vraie protection contre la copie serait exigée contractuellement — à réévaluer alors. **Non exercé en vrai** : migration V014 pas encore appliquée sur Supabase (pas d'accès aux credentials dans cette session) — syntaxe/logique/RLS vérifiés par `check-emprunt` + `node --check`, mais le flux complet (emprunt → lecture → retour → file d'attente) reste à tester après application de la migration.
- **2026-07-19 — fix ⚠️** : **découverte — V007 à V013 n'avaient en réalité jamais été appliquées sur Supabase**, malgré les entrées précédentes de ce journal affirmant leur application (16/07). Seule `acces_premium` (schéma de base) existait ; `livres`/`livre_editions`/`livre_offres`/`commandes_occasion`/`cadeaux`/`oeuvre_etageres`/`emprunts`/`emprunts_file_attente` étaient absentes (`relation "livre_offres" does not exist` à la première tentative d'appliquer V014). Franklin a exécuté manuellement le bundle **V007 → V014** dans l'éditeur SQL Supabase : les 10 tables attendues sont désormais confirmées présentes. **Leçon retenue** : ne plus marquer une migration « appliquée » dans ce journal sans une vérification réelle en base (une requête `information_schema.tables` a suffi à révéler l'écart) — les entrées passées relayaient une déclaration d'intention, pas un fait vérifié. Conséquence positive : **P4 #15 (Emprunter) est maintenant opérationnel en base**, le flux emprunt → lecture → retour → file d'attente reste à exercer en conditions réelles (créer une offre `pret_numerique` avec `stock`/`duree_acces_jours` sur un livre pour tester).
- **2026-07-17 — feat** : P3.12 complété — historique de commandes lecteur : API `getAchatsUtilisateur`, section **Mes achats** dans la bibliothèque locale/offline, cartes des livres premium confirmés Fapshi avec lien de lecture, contrôle `check-purchase-history`.
- **2026-07-17 — feat** : P3.12 livré — panier numérique progressif + checkout Fapshi multi-livres : module `cart.js`, ajout panier sur fiche œuvre premium, page paiement `?cart=1`, `/api/fapshi-pay` crée une ligne paiement par livre, webhook confirme toutes les lignes d'une référence et active chaque accès premium, contrôle `check-cart-checkout`.
- **2026-07-17 — feat** : P2.11 livré — reporting auteur niveau KDP renforcé : API `getReportingAuteur`, dashboard avec ventes premium, pages suivies, royalties 50/50, seuil payout Mobile Money, Select affiché comme option en réserve, montants en XAF et contrôle `check-author-reporting`.
- **2026-07-17 — feat** : P2.10 livré — couche sociale lecteur : migration `V010__etageres_sociales_stats.sql` (`oeuvre_etageres`, RPC `get_oeuvre_social_stats`), API stats/étagères, fiche œuvre avec compteurs avis/à lire/en cours/terminés/favoris, actions “À lire / En cours / Terminé / Favori”, marquage automatique “en cours” à la lecture et contrôle `check-social-layer`.
- **2026-07-16 — feat** : P2.9 livré — accueil en **rails de lecture/commerce** et fiche livre unifiée : API `getOffresLivre`/`getRailsMarchands`, rails “Les plus lus / Nouveautés / Gratuit / Premium”, bloc offres sur `work.html` (lecture, achat Fapshi, hors ligne, emprunt et occasion préparés), recommandations nettoyées au design-system et contrôle `check-commerce-rails`.
- **2026-07-16 — feat** : P1.8 livré — traduction ancrée sur `chapitre_id` stable : migration `V009__traductions_chapitres_stables.sql`, cache `chapitre_ref + langue_source + source_hash`, lecteur passant l'objet chapitre complet au traducteur, fallback compatible avec l'ancien `chapitre_id` technique et contrôle `check-translation-normalization`.
- **2026-07-16 — feat** : P1.7 livré — formulaire de publication aligné standards KDP/Kobo : sous-titre, série, catégories, mots-clés (max 7), ISBN, éditeur, territoires, protection numérique, checklist bloquante à 85 %, couverture obligatoire avec ratio vertical 1,6:1, métadonnées synchronisées dans `livres.metadata` et contrôle `check-publish-standards`.
- **2026-07-16 — infra** : P1.6 backfill ajouté — commande `npm run epub:backfill` (`scripts/backfill_epub_editions.mjs`) pour générer les EPUB canoniques des œuvres existantes, uploader `oeuvres/{id}/canonique.epub`, synchroniser `livres`, `livre_editions` et `livre_offres`. Mode test par défaut, écriture uniquement avec `--apply`.
- **2026-07-16 — infra** : P1.6 validation préparée — ajout de `scripts/validate_epub.mjs`, commande `npm run epub:validate` et mémo `tools/epubcheck/README.md` pour lancer l'epubcheck officiel dès que Java + `epubcheck.jar` sont disponibles. Sur cette machine, Java n'est pas installé, donc la validation stricte reste à activer côté environnement.
- **2026-07-16 — feat** : P1.6 avancé — publication reliée au pipeline commerce/EPUB : génération d'un **EPUB canonique navigateur** (`assets/js/epub-builder.js`), upload privé `canonique.epub`, synchronisation automatique `livres`, `livre_editions` (`chapitres` + `epub`) et `livre_offres` pour chaque nouvelle publication, plus contrôle de régression. Cache PWA `kala-v22`.
- **2026-07-16 — feat** : P1.6 avancé — lecteur **EPUB web** branché en parallèle du lecteur chapitres : détection d'un fichier `.epub` ou d'une édition `livre_editions`, URL signée Supabase si stockage privé, rendu `epub.js`, mêmes contrôles page suivante/précédente, thèmes/polices/interligne appliqués, fallback automatique vers le lecteur Kalamundi classique et contrôle de régression mis à jour. Cache PWA `kala-v21`.
- **2026-07-16 — feat** : P1.6 en cours — socle **chapitres normalisés + EPUB canonique** livré : migration `V008__chapitres_normalisation_epub.sql` (`chapitre_id`, `source_hash`, `epub_href`, `metadata`), librairie `scripts/lib/book-normalizer.mjs`, générateur `scripts/build_epub.mjs`, contrôle `check-epub-pipeline`, publication enrichie avec identifiants stables et cache PWA bumpé en `kala-v20`. Reste : intégration lecteur EPUB web Readium/foliate + validation epubcheck complète.
- **2026-07-16 — feat** : P1.5 livré — décision **D3 Livre + Offres** validée et migration `V007__livres_offres.sql` créée : tables `livres`, `livre_editions`, `livre_offres`, backfill depuis `oeuvres`, liens progressifs vers `paiements`, `revenus`, `acces_premium`, RLS et index commerce.
- **2026-07-16 — design** : P0.4 livré — intégration du logo : mark **KM + plume + livre** dérivé en `assets/img/logo-mark-km.png`, icônes PWA régénérées via `scripts/generate-icons.mjs`, remplacement des logos emoji dans les nav/footer et owner dashboard, cache PWA bumpé en `kala-v19`.
- **2026-07-16 — fix** : P0.3 livré — hygiène lecteur : démarrage en ligne fiabilisé après la refonte offline, fallback chapitres depuis l'œuvre, contrôle de régression lecteur/Roboto ajouté au `npm run check`, absence de Roboto confirmée et cache PWA bumpé en `kala-v18`.
- **2026-07-16 — design** : piste logo retenue et enregistrée : monogramme **KM** lisible avec plume centrale, petit livre discret, wordmark `KalaMundi` réduit et signature `La Plume du Monde` plus littéraire. Asset : `assets/img/logo-kalamundi-km-plume.png`. Prompt mis à jour dans `PROMPT_LOGO_KALAMUNDI.md`.
- **2026-07-16 — feat** : P0.2 livré — interface auteur modernisée : dashboard enrichi par un bloc standard auteur (50/50, statistiques, dépôt), formulaire publication doté d'un cockpit qualité dynamique, résumé obligatoire, validation fichier réelle, rappel couverture 1,6:1 et cache PWA bumpé en `kala-v15`.
- **2026-07-16 — feat** : P0.1 livré — lecteur premium renforcé : choix de police de lecture, thèmes jour/nuit/sépia appliqués proprement, réglages qui repaginent le livre, note d'annotation/surlignage visible et cache PWA bumpé en `kala-v14`.
- **2026-07-16 — feat** : **P3 #13 (Diaspora) — UI cadeau**. Bouton « 🎁 Offrir » sur la fiche œuvre premium (`work.js`) → `payment.html?cadeau=1`. Mode cadeau dans le paiement (`payment.js`/`payment.html`) : champs destinataire + petit mot, envoi `cadeau:true`, puis affichage du **code** au retour de paiement avec copie + partage WhatsApp. Nouvelle page de réclamation `pages/reclamer.html` + `assets/js/reclamer.js` : saisie du code (tirets/espaces ignorés, pré-remplissage via `?code=`), appel RPC `reclamer_cadeau`, messages d'erreur métier explicites, redirection lecteur au succès. SW `kala-v29` (work.js modifié). **UI non exercée en vrai (nécessite déploiement + V011 appliquée) ; syntaxe/câblage/logique vérifiés.** Reste #13 : connecteur international cartes/PayPal (bloqué : compte marchand). `fapshi-pay.js` gère `cadeau:true` : crée une ligne `cadeaux` (code généré serveur, montant XAF tracé) au lieu d'un accès acheteur, renvoie le code. `fapshi-webhook.js` confirme le cadeau (`en_attente`→`paye`) et **crédite l'auteur** (50 %, `vente_cadeau`) sans donner d'accès à l'acheteur — le bénéficiaire réclame via la RPC `reclamer_cadeau`. Crédit auteur factorisé (`crediterAuteur`), idempotent. Vérifié de bout en bout par `check-gift-flow.mjs` (mock Fapshi+Supabase : achat → pas d'accès acheteur, webhook → cadeau payé + auteur crédité 3280 pour 10 EUR, re-webhook → pas de double traitement). **Reste #13 : UI (achat cadeau + page de réclamation) et connecteur international cartes/PayPal (bloqué : compte marchand requis).**
- **2026-07-16 — feat** : **économie & frais Fapshi explicites**. Recherche tarifs Fapshi : **encaissement 3 %, payout 0 % (gratuit)**, topup 2,2 %, aucun frais fixe/abonnement (tarif public, taux négocié possible sur dashboard ; sur-mesure > 100 M FCFA). Nouveau `scripts/lib/economie.mjs` : répartition livre (50/50, D10), occasion (15 %, D15, aucun revenu auteur, vendeur payé plein car payout gratuit → plateforme absorbe les 3 %), et **promo** (`repartirVentePromo` : remise + part plateforme majorée, idée Franklin D17). Contrôle `check-economie` (invariant : parts + frais = brut, aucun franc perdu). A corrigé une erreur de calcul occasion (vendeur = prix − commission, pas net − commission).
- **2026-07-16 — decision ✅** : **D15 tranchée — commission occasion 20 %** (niveau PangoBooks), la plateforme porte les frais Fapshi. Appliqué : `occasion-etats.mjs`, `economie.mjs`, `V012` (SQL), tests `check-occasion`/`check-economie`. Nouvel `apercuVendeurOccasion()` (répartition claire vendeur). **Documentée pour le vendeur** : CGU §5.5 (tableau 1000/2000/5000 FCFA, « aucun coût caché »). Exemple : occasion 2000 → vendeur 1600, Kalamundi 340 (commission 400 − frais 60).
- **2026-07-16 — decision ✅** : **D16 tranchée — la plateforme absorbe seule les frais Fapshi** (défaut `economie.mjs` = `plateforme`), l'auteur touche sa part **pleine** (500 sur 1000). Documenté visiblement côté auteur : `pages/publish.html` (carte Premium), `pages/contrat-auteur.html` (§5.1 + encart frais), `assets/js/author-dashboard.js` (détail royalties).
- **2026-07-16 — feat** : **P4 #14 — payout vendeur confirmé + client codé**. Recherche : Fapshi expose `POST /payout` (disbursement Mobile Money MTN/Orange + compte Fapshi + bulk ; en-têtes apiuser/apikey ; min 100 XAF ; réponse transId). ⚠️ **payout désactivé par défaut en live → activation à demander au support Fapshi + float requis**. Client `scripts/lib/fapshi-payout.js` (construction validée + envoi injectable), contrôle `check-payout` (endpoint, en-têtes, corps, refus). Le pilier « Vendre » est donc viable avec Fapshi seul, sans tiers. Reste l'orchestration serveur du versement (déclenchée à la clôture).
- **2026-07-16 — feat** : **P4 #14 — paiement d'occasion sous séquestre**. `fapshi-pay` gère `commandeOccasionId` : le montant est **relu depuis la commande** (jamais depuis le client — argent entre inconnus), l'acheteur est vérifié (403 sinon), le double paiement bloqué (409), et la commande est reliée au paiement (`paiement_id = transId`). `fapshi-webhook` : au paiement réussi, la commande passe en **`paye_sequestre`** (argent gelé) — **aucun versement au vendeur** ici (il n'aura lieu qu'à la confirmation de réception, V012). Idempotent. Contrôle `check-occasion-flow` (5 scénarios : montant serveur, commande d'autrui, double paiement, gel séquestre sans payout, rejeu).
- **2026-07-16 — feat** : **P4 #14 — page commande, découverte et boucle complète**. `pages/commande.html` + `assets/js/commande.js` : timeline du séquestre (créée → payée/gelée → remise → reçue → close) et actions **strictement selon le rôle réel** (acheteur/vendeur déduit de la commande, jamais d'un paramètre d'URL) — payer, confirmer remise (vendeur), confirmer réception *« libère le paiement au vendeur »* (acheteur), ouvrir un litige, évaluer en fin de parcours. `payment.js` : mode `occasion=1&commande=…` qui appelle `fapshi-pay` avec `commandeOccasionId` et renvoie vers `commande.html` après paiement. Fiche œuvre (`work.js`) : la carte **« Occasion »** n'est plus un « Bientôt » — elle liste les annonces réelles (`api.getOffresOccasion`) et permet de réserver en un clic (`api.reserverOccasion` → RPC `creer_commande_occasion`). **Le cycle complet est bouclé** : découvrir → réserver → payer → séquestre → remettre → recevoir → fonds libérés → évaluer. Contrôle `check-commande-occasion` (99 fichiers JS, rien de cassé sur les rails commerce / couche sociale).
- **2026-07-16 — decision ✅** : **V011/V012/V013 appliquées sur Supabase** (bundle `_bundle_V011_V012_V013.sql`) — gifting et mise en vente d'occasion opérationnels de bout en bout (hors payout, en attente d'activation Fapshi).
- **2026-07-16 — feat** : **P4 #14 — paiement occasion branché (séquestre)**. `fapshi-pay.js` : pour une commande occasion, le **montant est relu côté serveur** depuis `commandes_occasion` (jamais fait confiance au client), vérifie que l'acheteur est bien le sien et que la commande est encore payable, puis relie le paiement (`paiement_id = transId`). `fapshi-webhook.js` : au paiement confirmé, la commande passe en **`paye_sequestre`** (l'argent est gelé) — **aucun versement au vendeur à ce stade** (il n'aura lieu qu'à la confirmation de réception, V012). Idempotent. Contrôle `check-occasion-flow` (montant serveur, commande d'autrui refusée 403, double paiement refusé 409, séquestre gelé, pas de payout prématuré, idempotence).
- **2026-07-16 — feat** : **P4 #14 — formulaire de mise en vente d'occasion**. Migration `V013__annonce_occasion.sql` : RPC `creer_annonce_occasion` SECURITY DEFINER qui retrouve la fiche `livres` par ISBN ou en crée une minimale (catalogue `import`, sans auteur — un vendeur n'est pas l'auteur), puis crée l'offre `occasion` rattachée au vendeur (état, ville, mode remise, photos dans `conditions`). Page `pages/vendre.html` + `assets/js/vendre.js` : formulaire avec **répartition en direct** (le vendeur voit « vous recevez » se mettre à jour à la saisie du prix ; 20 %, frais portés par Kalamundi). Entrée « 🏷️ Vendre un livre » ajoutée au menu profil (`app.js`). Contrôle `check-vendre`.
- **2026-07-16 — feat** : **P4 #14 (Vendre/occasion) — socle séquestre**. Machine à états `scripts/lib/occasion-etats.mjs` (en_attente_paiement → paye_sequestre → remis → receptionne → clos, + litige/annule/rembourse ; gardes par acteur ; auto-libération 7 j ; commission 15 % ; **aucun revenu auteur** sur l'occasion = doctrine de la 1re vente). Migration `V012__occasion_sequestre.sql` : table `commandes_occasion` (fonds gelés par défaut, payout_statut), `vendeur_evaluations` (réputation publique), 5 RPC SECURITY DEFINER (creer_commande_occasion / confirmer_remise / confirmer_reception / ouvrir_litige / evaluer_vendeur) qui répliquent les règles côté serveur ; aucune écriture client directe. Le listing réutilise `livre_offres` type `occasion`. Contrôle `check-occasion.mjs`. **Reste #14 : versement vendeur (payout — bloqué sur capacité Fapshi payout), UI annonce/commande, branchement webhook → paye_sequestre.**
- **2026-07-16 — feat** : **P3 #13 (Diaspora) — gifting**. Migration `V011__cadeaux_diaspora.sql` : table `cadeaux` (code unique, devise contrainte, montant XAF encaissé tracé, statuts en_attente/paye/reclame/annule, contrainte de cohérence de réclamation), RLS, et **RPC `reclamer_cadeau` SECURITY DEFINER** (verrou `FOR UPDATE` contre la double réclamation, refus du cadeau non payé / déjà réclamé / offert à soi-même ; l'accès `acces_premium` va au **bénéficiaire**, pas à l'acheteur). Aucune écriture client directe. Codes cadeaux dans `scripts/lib/cadeaux.mjs` : tirage cryptographique sans biais, alphabet sans caractères confondables (ni 0/O, 1/I/L, U) car le code se dicte au téléphone, tolérant à la saisie (tirets/espaces/minuscules). Contrôle `check-cadeaux.mjs`. **Reste #13 : connecteur de paiement international (cartes/PayPal) + UI.**
- **2026-07-16 — feat** : **P3 #13 (Diaspora) — socle multi-devises**. Nouveau `scripts/lib/devises.mjs` : XAF base, **parité fixe légale 1 EUR = 655,957 XAF**, USD **flottant** configurable via `TAUX_USD_XAF`, formatage localisé, détail de conversion pour l'acheteur. Toute devise inconnue est **refusée** (plus de repli silencieux en XAF). `fapshi-pay.js` branché dessus. A corrigé 2 bugs graves : euro encaissé comme des XAF (facteur 655) et dollar converti à la parité de l'euro (+8-10 %). Contrôle `check-devises.mjs` (24 assertions, tripwire anti-655,957). Cf. `ERROR_LOG.md`.
- **2026-07-16 — feat** : **dette P1 #6 comblée — import serveur robuste**. Nouvel endpoint `/api/import-book` (`functions/api/import-book.js`) qui extrait DOCX/ODT/EPUB/HTML/TXT **sans aucune dépendance** (`scripts/lib/document-import.mjs` : lecteur ZIP via `DecompressionStream`, APIs standard du Web, compatible Workers) et réutilise le **normaliseur partagé** → `chapitre_id` identiques au client. `upload.js` tente le serveur puis **retombe silencieusement sur le parsing client** (hors-ligne). Évite de télécharger mammoth + epub.js + JSZip depuis un CDN à chaque import (plusieurs Mo, data chère). Le **PDF reste côté client** (pdf.js). Contrôle `check-book-import.mjs` ajouté à `npm run check` (DOCX deflate réel, ODT stored, EPUB, HTML, TXT, refus PDF/archives corrompues). **#6 terminé.**
- **2026-07-16 — fix** : **dette P1 #6 comblée côté validation** — ajout d'un validateur EPUB **natif** (`scripts/lib/epub-validator.mjs`, sans Java) branché dans `check-epub-pipeline` → `npm run check` valide désormais réellement chaque EPUB produit (OCF, OPF, manifest/spine, namespaces XML) ; epubcheck reste une passe profonde optionnelle. A révélé et corrigé 4 bugs : EPUB invalides (`epub:` non déclaré, les 2 builders), validation jamais exécutée, `npm run epub:build` inopérant sur Windows, `CRC_TABLE` en zone morte. Détails dans `ERROR_LOG.md`. **Reste #6 : import serveur robuste.**
- **2026-07-16 — docs** : ajout d'un **ordre d'exécution priorisé** (section E de `PLANIFICATION`, tiers P0→P5 avec dépendances). Pivot identifié : le modèle « Livre + Offres » (n°5) débloque le commerce → à faire tôt.
- **2026-07-16 — fix** : fiabilisation du mode hors-ligne lecteur : `offline.html` devient une bibliothèque locale listant les livres sauvegardés, le lecteur peut démarrer depuis IndexedDB sans réseau, et le cache PWA passe en `kala-v13`.
- **2026-07-16 — feat** : ajout d'une porte d'entrée `Kalamundi Campus` sur l'accueil, alignée avec D12 : accès clair aux annales, examens simulés, Super Répétiteur et espaces institutions, textes internationalisés et cache PWA bumpé en `kala-v12`.
- **2026-07-16 — feat** : ajout du shell mobile Phase 1 : barre d'onglets basse commune (Accueil · Explorer · Apprendre · Biblio · Profil) injectée sur les pages cœur, avec exclusions lecteur/paiement/login/admin/owner et cache PWA bumpé en `kala-v11`.
- **2026-07-16 — style** : application du socle design global Phase 1 : polices Fraunces/Inter auto-hébergées, tokens `base.css` modernisés (base crème, bordures neutres, rayons/ombres), composants communs adoucis (boutons, cartes, inputs, search, skeleton) et Service Worker bumpé en `kala-v10`.
- **2026-07-16 — decision ✅** : deux piliers stratégiques ajoutés — **D13 canal maisons d'édition** (comptes catalogue, ONIX/EPUB, revenus négociés, onboarding via collectifs) et **D14 mission culturelle** (Fonds patrimoine « Kalamundi Héritage » public-bien + partenariat MINAC/OIF/UNESCO). Nuance factuelle : le Cameroun a une BN + loi dépôt légal 2000 + programme MINAC PNNPC, mais pas d'accès numérique national → Kalamundi = couche d'accès partenaire, pas remplaçant. Doc : `VISION_EDITEURS_ET_PATRIMOINE.md`.
- **2026-07-16 — docs** : création de `DESIGN_SYSTEM_KALAMUNDI.md` (nouveaux tokens exacts pour `base.css`) + `INTERFACES_KALAMUNDI.md` (inventaire de tous les écrans par rôle + priorités). Référencés dans `AGENTS.md`.
- **2026-07-16 — docs** : création de `AGENTS.md` + `CLAUDE.md` (contrat de travail pour Codex/agents : documentation d'abord, journalisation obligatoire évolutions + erreurs, direction design, règles code/métier). Codex démarre la mise en œuvre.
- **2026-07-16 — decision ✅** : **D10 tranchée — royalties 50/50** (50 % auteur). Option « Kalamundi Select » 70 % gardée en réserve, non actée.
- **2026-07-16 — decision** : priorité transverse — **moderniser toutes les interfaces clés** (auteur, lecteur, boutique, éducation) selon `BIBLE §10`.
- **2026-07-15 — docs** : création du socle de pilotage — `BIBLE_KALAMUNDI.md`, `PLANIFICATION_KALAMUNDI.md`, `EVOLUTION_KALAMUNDI.md`, `ERROR_LOG.md`, en complément de `RECONCEPTION_KALAMUNDI.md` et `MEMO_MARKETPLACE_LIVRE.md`.
- **2026-07-15 — decision** : pivot stratégique acté — Kalamundi devient une « super-app du livre » (lire / apprendre / publier / acheter / vendre / emprunter). Décisions D0–D9 proposées, en attente de validation Franklin.
- **2026-07-15 — decision ✅** : **D9 validée par Franklin** — accepter tous formats (natif/Word/PDF/EPUB) → convertisseur « égalisateur » qui normalise en chapitres internes ET build un EPUB canonique. Lecteur web type Readium/Thorium. Traduction préservée par la normalisation (vigilances : nœuds de texte, langue source déclarée). Checklist de dépôt auteur (langue originale obligatoire). Sortie validée par epubcheck. Readium **LCP** = socle du futur prêt.
- **2026-07-15 — decision ✅** : **diaspora** intégrée à la cible (D11) — achat/location par la diaspora → paiement international (cartes/PayPal) + multi-devises (EUR/USD/FCFA) + **gifting** (offrir un livre livré à un proche au pays).
- **2026-07-15 — decision ✅** : **D12 validée** — éducation option (a) : intégrée maintenant, séparable plus tard en PWA dédiée **« Kalamundi Campus »** sur backend partagé. **D5 validée** — périmètre **Cameroun + diaspora d'abord**, extension aux autres pays africains si succès.
- **2026-07-15 — proposition** : **stratégie éducation** (D12) — recommandation « intégré maintenant, séparable plus tard en PWA dédiée sur backend partagé » (Kalamundi est un PWA → séparer coûte peu). Le produit doit se différencier des « apps à PDF » (quiz interactifs, corrigés IA, examen simulé, répétiteur, lien achat/location manuels, accessibilité type Eneza, canal écoles B2B). Doc : `PROPOSITION_EDUCATION.md`.
- **2026-07-15 — decision** : **adopter les standards des grandes plateformes** (KDP en référence) pour la publication — EPUB, métadonnées ONIX-like, couverture 1,6:1, royalties transparentes (50 % / 70 % « Kalamundi Select »), paiement à la page lue en abonnement, reporting auteur niveau KDP. Doc : `ADAPTATION_STANDARDS_KDP.md`. Décision **D10** (taux à confirmer). Print-on-demand et distribution externe reportés.
- **2026-07-05 — feat** : sélecteur de langue appliqué à toute l'interface *(commit `37b725e`)*.
- **2026-07-05 — fix** : filtrage de l'étagère « auteurs » corrigé *(commit `68dac55`)*.
- **2026-07 — feat** : stats de lecture « honnêtes » sur la home + filtres de collection de la bibliothèque clarifiés *(commits `de22baf`, `3cec321`, `c6f0913`)*.

## 2026-06

- **2026-06-23 — fix** : suppression d'œuvre déplacée côté serveur via Pages Function `/api/delete-oeuvre` (contournement RLS, fiabilisée).
- **2026-06-23 — perf** : optimisations majeures (parallélisme des requêtes, Service Worker minimal, `FETCH_LIMIT` 60).
- **2026-06-23 — feat** : meta OG dynamiques sur la page œuvre (partage social).
- **2026-06 — feat** : espace étudiant (annales, épreuves, examen simulé, répétiteur) *(migration `V003`)*, paywall chapitres gratuits *(`V006`)*, communautés/commentaires *(`V005`)*.

---

*Antérieur à juin 2026 : voir l'historique git (`git log`) pour le détail des débuts (publication, lecteur, abonnements Fapshi, PWA).*
